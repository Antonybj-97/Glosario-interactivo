<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Editar término</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">
</head>
<body>
  <section class="section">
    <div class="container">
      <h1 class="title">Editar término</h1>

      <form method="POST" action="/update/<%= word.id %>">
        <div class="field">
          <label class="label">Término</label>
          <div class="control">
            <input class="input" type="text" name="term" value="<%= word.term %>" required>
          </div>
        </div>

        <div class="field">
          <label class="label">Significado</label>
          <div class="control">
            <textarea class="textarea" name="meaning" required><%= word.meaning %></textarea>
          </div>
        </div>

        <div class="field is-grouped">
          <div class="control">
            <button class="button is-success">Actualizar</button>
          </div>
          <div class="control">
            <a href="/" class="button is-light">Cancelar</a>
          </div>
        </div>
      </form>
    </div>
  </section>
</body>
</html>
